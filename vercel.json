{
  "buildCommand": "DATABASE_PROVIDER=postgresql npx prisma migrate deploy && DATABASE_PROVIDER=postgresql prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/health",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/donations",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/audit-retention",
      "schedule": "0 4 * * 0"
    }
  ]
}
