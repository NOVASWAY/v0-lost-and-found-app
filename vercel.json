{
  "buildCommand": "DATABASE_PROVIDER=postgresql sh -c 'prisma migrate resolve --applied 20260809222858_init; prisma migrate resolve --applied 20260809222859_add_mission_meeting_minutes; prisma migrate deploy' && prisma generate && next build --webpack",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/donations",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/audit-retention",
      "schedule": "0 4 * * 0"
    }
  ]
}
