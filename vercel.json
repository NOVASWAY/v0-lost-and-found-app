{
  "buildCommand": "DATABASE_PROVIDER=postgresql prisma db push && prisma generate && DATABASE_PROVIDER=postgresql tsx prisma/seed.ts && next build --webpack",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/donations",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/audit-retention",
      "schedule": "0 4 * * 0"
    }
  ]
}
