{
  "buildCommand": "DATABASE_PROVIDER=postgresql prisma migrate deploy && prisma generate && next build --webpack",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/donations",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/audit-retention",
      "schedule": "0 4 * * 0"
    }
  ]
}
