{
  "buildCommand": "DATABASE_PROVIDER=postgresql prisma generate && if [ \"$VERCEL_ENV\" = \"production\" ]; then DATABASE_PROVIDER=postgresql prisma db push --accept-data-loss && DATABASE_PROVIDER=postgresql tsx prisma/seed.ts; fi && next build --webpack",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/donations",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/audit-retention",
      "schedule": "0 4 * * 0"
    }
  ]
}
